include("executor.jl")

function day04(lines)
    matches = 0

    # split(line, ";") -- String bei Zeichen ";" teilen. Beide Teile werden in einem Array gespeichert
    # parse(Int, "200") -- Konvertiert die Zeichenkette "200" in den entsprechenden Zahlwert 200
    # array[1] -- gibt den Wert bei Index 1 zurück
    # Mathematische Operatoren: += (etwas zuaddieren); <= (kleiner-gleich); >= (größer-gleich); ==

    for line in lines

        if true == true
        end

    end

    return matches
end

execute(4, day04)